# Copy to .env and fill in with your deployment values.

# Required: API key for your OpenAI-compatible endpoint.
OPENAI_API_KEY=replace-with-your-key

# Required if not using api.openai.com; include the /v1 suffix if your host expects it.
# Example: https://your-endpoint.example.com/v1
OPENAI_BASE_URL=https://your-endpoint.example.com/v1

# Model/deployment name to call.
OPENAI_MODEL=your-model-name
